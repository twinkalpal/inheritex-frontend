const SliceNames = {
	AUTH: 'auth',
};

const REDUX_PERSISTENT_STORE_ENCRYPT_KEY = 'redux$persistentStore$key';

const REDUX_PERSISTENT_STORE_KEY = 'root';

export { SliceNames, REDUX_PERSISTENT_STORE_ENCRYPT_KEY, REDUX_PERSISTENT_STORE_KEY };
